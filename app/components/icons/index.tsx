import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from '../../config/config.json';
const Icon = createIconSetFromFontello(fontelloConfig, 'adicticiconfont');
export default Icon;